package o;

import java.util.HashMap;
import java.util.Map;
/* loaded from: classes6.dex */
public final class majorAxisDistanceToFarEdge implements getViewHorizontalDragRange {
    private final onItemRangeInserted toString;

    public boolean equals(Object obj) {
        if (this == obj) {
            return true;
        }
        return (obj instanceof majorAxisDistanceToFarEdge) && runAnimators.values(this.toString, ((majorAxisDistanceToFarEdge) obj).toString);
    }

    public int hashCode() {
        return this.toString.hashCode();
    }

    public String toString() {
        return "ProfilePictureUploadError(_errorMessage=" + this.toString + ')';
    }

    public majorAxisDistanceToFarEdge(onItemRangeInserted onitemrangeinserted) {
        runAnimators.ag$a(onitemrangeinserted, "_errorMessage");
        this.toString = onitemrangeinserted;
    }

    @Override // o.getViewHorizontalDragRange
    public Map<String, Object> values() {
        HashMap hashMap = new HashMap();
        hashMap.put("eventName", "ProfilePictureUploadError");
        hashMap.put("errorMessage", this.toString.values());
        return hashMap;
    }
}
