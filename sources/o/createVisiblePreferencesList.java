package o;
/* loaded from: classes7.dex */
public class createVisiblePreferencesList extends createExpandButton {
    float valueOf = -1.0f;

    @Override // o.createExpandButton
    public void ag$a(areContentsTheSame arecontentsthesame, float f, float f2, float f3) {
        arecontentsthesame.toString(0.0f, f3 * f2, 180.0f, 180.0f - f);
        double d = f3;
        double d2 = f2;
        arecontentsthesame.toString((float) (Math.sin(Math.toRadians(f)) * d * d2), (float) (Math.sin(Math.toRadians(90.0f - f)) * d * d2));
    }
}
